/* Admin Landing Page. */
import * as React from 'react';
import AppBase from './components/AppBase';

export default function AdminApp() {

  React.useEffect(()=>{
    // Load Requisites when page Completes
  },[]);

  return (
    <AppBase bannerTitle="Audi-Hose">
     <h1>Test</h1>
    </AppBase>
  );
}

